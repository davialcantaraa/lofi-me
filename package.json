{
	"name": "lofi-me",
	"version": "0.1.0",
	"main": "packages/electron/main.js",
	"private": "true",
	"workspaces": {
		"packages": [
			"packages/*"
		]
	},
	"scripts": {
		"app-start": "lerna run start",
		"app-build": "lerna run build",
		"app-test": "lerna run test",
		"app-eject": "lerna run eject",
		"electron-dev": "lerna run electron",
		"electron-build": "lerna run electron-build",
		"web-dev": "lerna run web",
		"web-build": "lerna run web-build",
		"server-dev": "lerna run dev",
		"diff": "lerna diff"
	},
	"dependencies": {
		"lerna": "^4.0.0"
	},
	"eslintConfig": {
		"extends": [
			"react-app",
			"react-app/jest"
		]
	},
	"browserslist": {
		"production": [
			">0.2%",
			"not dead",
			"not op_mini all"
		],
		"development": [
			"last 1 chrome version",
			"last 1 firefox version",
			"last 1 safari version"
		]
	}
}
